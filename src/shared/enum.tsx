export enum TransactionType {
  New,
  Update,
  Cancel
}

export enum ReferenceType {
  RFP
}

export enum Currency {
  USD
}

export enum MediaType {
  Tv = 0,
  Radio = 1
}

export enum ContactType {
  AccountExecutive,
  SalesPlanner,
  SalesAssistant,
  Buyer,
  Payee,
  Creative,
  DeliveryService
}

export enum MediaOutletType {
  SpotTV
}
export enum PricingMetricSegments {
  CPM,
  CPP,
  CPE,
  CPA,
  Custom
}

export enum RevenueType {
  Cash,
  Barter,
  Trade,
  Custom
}

export enum MpaaGuidelines {
  TV_Y,
  TV_Y7,
  TV_G,
  TV_PG,
  TV_14,
  TV_MA
}

export enum RatingStream {
  ProgramLive,
  C3,
  C7,
  Custom
}

export enum DistributionType {
  Quarter,
  Daypart,
  Market,
  Station,
  Program,
  Custom
}

export enum DistributionMethod {
  GRPs,
  Impressions,
  Units,
  Dollars
}
